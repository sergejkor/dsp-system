# KFZ Schadenanzeige PDF template

Place your blank form here as:

**`KFzSchadenanzeige_Template.pdf`**

The damages “Save & send” / insurance report flow loads this file to fill AcroForm fields (or draw text as fallback).

Alternatively set an absolute path in **`KFZ_TEMPLATE_PATH`** in `backend/.env`.
